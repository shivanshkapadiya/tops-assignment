#include<stdio.h>
#include<math.h>
int main()
{
	int num,rev,i,rem,p,a,ori,arm;
	printf("input the total num digit");
	scanf("%d",&p);
	printf("input the num");
	scanf("%d",&num);
	ori==num;
	while(num!=0)
	{
		
		rem=num%10;
		arm=arm+(rem,p);
		num=num/10;

		//rev=num/10
		
		
		
	}
	// num==0
	// num%rev
	if(ori==num)
	{
		printf("it a pal");
	}
	else
	{
	
	printf(" it not pal");
}
	
	return 0;
}
